# SMTP configuration for sending confirmation emails via Gmail.
# Generate an app password in your Google account and paste it below.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"

# Optional: override the "from" email address.
SMTP_FROM_EMAIL="BrandingBeez.io <your-email@gmail.com>"
